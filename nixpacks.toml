# Railway nixpacks.toml - 安装 Playwright + Chromium

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install playwright",
  "playwright install chromium"
]

[start]
cmd = "python main.py"
